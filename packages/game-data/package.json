{
  "version": "0.0.0-development",
  "name": "@packages/game-data",
  "private": true,
  "main": "index.ts",
  "dependencies": {
    "@packages/chart-parser": "0.0.0-development",
    "@packages/types": "0.0.0-development",
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "fs-extra": "^10.0.0",
    "music-metadata": "^7.12.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/fs-extra": "^9.0.13"
  },
  "scripts": {
    "start": "ts-node index.ts",
    "test": "echo 'ok'",
    "build": "yarn tsc && cp -r songs dist",
    "build-metadata": "yarn ts-node --esm scripts/generateMetadata.ts",
    "check-ts": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
