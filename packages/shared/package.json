{
  "version": "0.0.0-development",
  "name": "@packages/shared",
  "private": true,
  "main": "index.js",
  "module": "./src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest --run",
    "check-ts": "tsc --noEmit",
    "build": "yarn tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "4.7.4",
    "vitest": "0.25.1"
  }
}
